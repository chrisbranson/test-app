<div class="row">
	<div class="col-sm-2"></div>
	<div class="col-sm-8">
		<div class="row">
			<div class="col-sm-4">
				<%= render 'profile' %>
			</div>

			<div class="col-sm-8">
				<%= render 'follow_form' if signed_in? %>
				<%= render 'feed' %>
			</div>
		</div>
	</div>
	<div class="col-sm-2"></div>
</div>
